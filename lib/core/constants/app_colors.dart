import 'dart:ui';

/// Application color palette constants.
/// Contains all the color values used throughout the app.
class AppColors {
  AppColors._();

  /// Primary brand color - coral red
  static const Color primary = Color(0xFFD75F5A);

  /// Secondary accent color - blue
  static const Color secondary = Color(0xFF4E8CF2);

  /// Background color - off-white
  static const Color background = Color(0xFFFEFEFE);

  /// Divider color - light gray
  static const Color divider = Color(0xFFEDEDED);

  /// Primary text color - dark gray
  static const Color text = Color(0xFF222222);

  /// Secondary text color - medium gray
  static const Color textSecondary = Color(0xFF727272);

  /// White color for contrast elements
  static const Color white = Color(0xFFFFFFFF);

  /// Error color
  static const Color error = Color(0xFFE53935);

  /// Success color
  static const Color success = Color(0xFF43A047);
}
